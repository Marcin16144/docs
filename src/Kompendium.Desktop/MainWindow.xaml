<Window x:Class="Kompendium.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:Kompendium.Desktop"
        Title="Kompendium — Baza wiedzy" Height="780" Width="1240"
        WindowStartupLocation="CenterScreen" Background="#0f172a">
  <Window.Resources>
    <SolidColorBrush x:Key="Bg" Color="#0f172a"/>
    <SolidColorBrush x:Key="Surface" Color="#1e293b"/>
    <SolidColorBrush x:Key="Border" Color="#334155"/>
    <SolidColorBrush x:Key="Text" Color="#e2e8f0"/>
    <SolidColorBrush x:Key="Muted" Color="#94a3b8"/>
    <SolidColorBrush x:Key="Accent" Color="#22d3ee"/>

    <Style TargetType="Button" x:Key="Tool">
      <Setter Property="Background" Value="{StaticResource Surface}"/>
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="9,5"/>
      <Setter Property="Margin" Value="0,0,6,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border CornerRadius="7" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <HierarchicalDataTemplate DataType="{x:Type local:NavSection}" ItemsSource="{Binding Pages}">
      <TextBlock Text="{Binding Header}" Foreground="{StaticResource Text}" FontWeight="SemiBold" Margin="0,2"/>
    </HierarchicalDataTemplate>
    <DataTemplate DataType="{x:Type local:NavPage}">
      <StackPanel>
        <TextBlock Text="{Binding Header}" Foreground="{StaticResource Text}" TextTrimming="CharacterEllipsis"/>
        <TextBlock Text="{Binding Sub}" Foreground="{StaticResource Muted}" FontSize="10.5"
                   TextTrimming="CharacterEllipsis" Visibility="{Binding SubVisible}"/>
      </StackPanel>
    </DataTemplate>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="340" MinWidth="240" x:Name="SidebarCol"/>
      <ColumnDefinition Width="4"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!-- Sidebar -->
    <DockPanel Grid.Column="0" Background="{StaticResource Bg}" Margin="10,10,4,10">
      <StackPanel DockPanel.Dock="Top">
        <TextBlock Text="📚 Kompendium" Foreground="{StaticResource Accent}" FontSize="17" FontWeight="Bold"/>
        <TextBlock x:Name="SubHead" Text="Baza wiedzy" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,1,0,8"/>
        <Border Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="9" Padding="8,4" Margin="0,0,0,8">
          <DockPanel>
            <TextBlock Text="🔎" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="2,0,6,0"/>
            <TextBox x:Name="Search" Background="Transparent" BorderThickness="0" Foreground="{StaticResource Text}"
                     CaretBrush="{StaticResource Accent}" VerticalContentAlignment="Center" FontSize="13"
                     TextChanged="Search_TextChanged"/>
          </DockPanel>
        </Border>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
          <Button Content="🏠 Start" Style="{StaticResource Tool}" Click="Home_Click"/>
          <Button Content="⤴️ Wznów" Style="{StaticResource Tool}" Click="Resume_Click"/>
          <Button Content="↻" Style="{StaticResource Tool}" Click="Reindex_Click" ToolTip="Przebuduj indeks"/>
        </StackPanel>
      </StackPanel>
      <TreeView x:Name="Tree" Background="{StaticResource Bg}" BorderThickness="0" Foreground="{StaticResource Text}"
                SelectedItemChanged="Tree_SelectedItemChanged" VirtualizingStackPanel.IsVirtualizing="True">
        <TreeView.ItemContainerStyle>
          <Style TargetType="TreeViewItem">
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Margin" Value="0,1"/>
          </Style>
        </TreeView.ItemContainerStyle>
      </TreeView>
    </DockPanel>

    <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="{StaticResource Border}"/>

    <!-- Content -->
    <DockPanel Grid.Column="2" Background="{StaticResource Bg}">
      <Border DockPanel.Dock="Top" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Padding="8,6">
        <DockPanel>
          <Button Content="◀" Style="{StaticResource Tool}" Click="Back_Click"/>
          <Button Content="▶" Style="{StaticResource Tool}" Click="Forward_Click"/>
          <Button Content="🔎 Szukaj (Ctrl+K)" Style="{StaticResource Tool}" Click="InPageSearch_Click"/>
          <TextBlock x:Name="Address" DockPanel.Dock="Right" Foreground="{StaticResource Muted}" VerticalAlignment="Center"
                     Margin="8,0" TextTrimming="CharacterEllipsis"/>
        </DockPanel>
      </Border>
      <Border DockPanel.Dock="Bottom" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="0,1,0,0" Padding="10,4">
        <DockPanel>
          <TextBlock x:Name="Status" Foreground="{StaticResource Muted}" FontSize="11" Text="Gotowy"/>
          <TextBlock x:Name="Progress" DockPanel.Dock="Right" Foreground="{StaticResource Accent}" FontSize="11" Text=""/>
        </DockPanel>
      </Border>
      <wv2:WebView2 x:Name="Web"/>
    </DockPanel>
  </Grid>
</Window>
